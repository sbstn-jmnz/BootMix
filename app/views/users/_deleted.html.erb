<h1>Listing Deleted Users</h1>
<table class = "table table-striped table-bordered table-hover table-condensed">
  <thead>
    <tr class = "info">
      <th>Name</th>
      <th colspan="4">Action</th>
    </tr>
  </thead>

  <tbody>
    <% @deletedUsers.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Restore', restore_user_path(user.id), :method => :patch %></td>
        <td><%= link_to 'Really Destroy', reallyDestroy_user_path(user.id), :method => :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
